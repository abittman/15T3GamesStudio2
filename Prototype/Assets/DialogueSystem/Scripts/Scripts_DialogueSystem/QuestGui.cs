using UnityEngine;
using System.Collections;

/*
 * Basic handler for Quest Gui Items
 * 	-> Will show a tool tip when mouse over
 * 	-> When clicked, will open up the quest window (stretch goal)
*/

namespace DialogueSystem {
	public class QuestGui : MonoBehaviour {

		// Use this for initialization
		void Start () {
		
		}
		
		// Update is called once per frame
		void Update () {
		
		}
	}
}
